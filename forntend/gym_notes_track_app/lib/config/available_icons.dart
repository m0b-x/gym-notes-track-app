import 'package:flutter/material.dart';

/// Available icons for custom markdown shortcuts
class AvailableIcons {
  static const List<IconData> all = [
    Icons.tag,
    Icons.star,
    Icons.favorite,
    Icons.lightbulb,
    Icons.warning,
    Icons.info,
    Icons.check_circle,
    Icons.highlight,
    Icons.palette,
    Icons.bookmark,
    Icons.label,
    Icons.flag,
    Icons.push_pin,
    Icons.note,
    Icons.description,
    Icons.article,
    Icons.menu_book,
    Icons.attachment,
    Icons.local_offer,
    Icons.style,
    Icons.fitness_center, // Barbell/dumbbell icon
  ];

  // Private constructor to prevent instantiation
  AvailableIcons._();
}
